package convert;

public class ConverterDriver {
      public static void main(String[]args) {
    	  DistanceConvert distobj=new DistanceConvert();
    	  distobj.Convert();
    	  Timeconverter timeobj=new Timeconverter();
    	  timeobj.convert();
    	  
      }
}
